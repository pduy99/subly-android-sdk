cmake_minimum_required(VERSION 3.22)
project(vad_probe CXX C)
set(CMAKE_CXX_STANDARD 17)
set(SRC /home/helios/Code/subly-project/subly-android-sdk/asr/whisper/src/main/cpp)
include_directories(${SRC} ${SRC}/ggml/include ${SRC}/ggml/src)
add_subdirectory(${SRC}/ggml ggml_build)
add_executable(vad_probe ${SRC}/whisper.cpp probe.cpp)
target_compile_definitions(vad_probe PRIVATE GGML_USE_CPU WHISPER_VERSION="probe")
target_link_libraries(vad_probe ggml)
